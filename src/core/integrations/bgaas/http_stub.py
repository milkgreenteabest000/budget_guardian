# 之後掛 FastAPI / uvicorn；目前不引入第三方套件，僅保留介面佔位。


def create_http_app():
    """
    TODO: from fastapi import FastAPI
    app = FastAPI()
    @app.post("/authorize") ...
    return app
    """
    raise NotImplementedError("PLACEHOLDER: wire FastAPI when dependencies are added")
